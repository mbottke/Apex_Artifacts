call_analysis/*.png
call_analysis/print_cal_*.html
call_analysis/artifact_data_*.json
# transient QA / sub-agent scratch files
qa_*.py
/*.png
/*.pdf

# Vercel CLI local project link (machine-local, not for the repo)
.vercel

# Claude Code local settings — may hold secrets (e.g. tokens); never commit
.claude/settings.local.json
